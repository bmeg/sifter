
class: Playbook
name: GraphManifest

steps:
  - desc: Download Manifest
    download:
      source: "{{inputs.url}}"
      dest: input.manifest
      output: manifest
  - desc: Loading Files
    manifestLoad:
      input: "{{inputs.manifest}}"
      baseURL: "{{inputs.baseURL}}"
