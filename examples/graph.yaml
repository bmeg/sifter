
class: Playbook
name: GraphManifest

steps:
  - desc: Download Manifest
    download:
      source: "{{input.url}}"
      dest: input.manifest
      output: manifest
  - desc: Loading Files
    manifestLoad:
      input: "{{input.manifest}}"
      baseURL: "{{input.baseURL}}"
