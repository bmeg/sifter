
class: Playbook
name: GraphManifest

prep:
  - argsCopy: input.manifest

steps:
  - desc: Loading Files
    manifestLoad:
      input: input.manifest
      #baseURL: "https://storage.googleapis.com/data.bmeg.io/bmeg-data.20190228/"
      baseURL: "http://localhost:8080/"
