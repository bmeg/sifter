
name: gdc-projects

outdir: output-tsv/

params:
  cases:
    type: File
    default: ../../resources/case.json
  diseaseTSV:
    type: File
    default: ../../resources/disease_table.tsv

inputs:
  caseReader:
    jsonLoad:
      input: "{{params.cases}}"

pipelines:
  tranform:
    - from: caseReader
    - fieldProcess:
        field: project
    - lookup:
        replace: disease_type
        tsv:
          input: "{{params.diseaseTSV}}"
          header:
            - disease
            - mondo
          key: disease
          value: mondo
    - emit:
        name: case-mondo